/// Stores various metrics information. e.g. number of datagrams/fragments sent, bandwidth
/// calculations, etc
pub struct Metrics {}

impl Metrics {
    pub fn new() -> Self {
        Self {}
    }
}
